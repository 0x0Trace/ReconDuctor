# =============================================================================
# ReconDuctor v2 - Dependencies
# =============================================================================
# Install: pip install -r requirements.txt && pip install -e .
# Or just: pip install -e .  (dependencies are also in pyproject.toml)
# =============================================================================

# Core
pydantic>=2.0.0
pydantic-settings>=2.0.0
structlog>=23.0.0
aiosqlite>=0.19.0
httpx>=0.25.0
typer>=0.9.0
rich>=13.0.0
jinja2>=3.1.0
PyYAML>=6.0
aiofiles>=23.0.0

# Favicon hashing (origin IP discovery via Shodan)
mmh3>=4.0.0
